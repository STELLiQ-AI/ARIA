# ARIA AAR Model Fine-Tuning Dependencies
# Install: pip install -r requirements.txt
# Requires: Python 3.10+, CUDA 12.x, RTX 3070 Ti (8GB VRAM)

# PyTorch with CUDA (install first)
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchvision
torchaudio

# Unsloth — 2x faster fine-tuning, 70% less VRAM
unsloth[colab-new]>=2024.8

# Hugging Face ecosystem
transformers>=4.44.0
datasets>=2.20.0
trl>=0.9.0
peft>=0.12.0
accelerate>=0.33.0
bitsandbytes>=0.43.0

# GGUF export
llama-cpp-python>=0.2.0

# Data generation (optional — requires ANTHROPIC_API_KEY)
anthropic>=0.34.0

# Utilities
tqdm
jsonlines
